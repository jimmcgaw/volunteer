<div style="float:right;">
	<%= button_to 'Delete Event', @event, :confirm => 'Are you sure you want to delete this event?', :method => :delete %>
</div>

<h1>Editing <%= @event.name %></h1>
<br />
<%= render 'form' %>

<%= link_to 'Show', @event %> |
<%= link_to 'Back', events_path %>
